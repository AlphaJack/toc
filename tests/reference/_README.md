Files is this folder have been created by running

```bash
file=name_of_file
./toc/cli.py -o "./tests/reference/$file" -f "./tests/input/$file"
```

and checking that the reference file has the expected content
